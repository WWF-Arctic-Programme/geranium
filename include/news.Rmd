## {data-width=400}

### NEWS

+ 2022-08-02

  + *(New)* Sets of predefined regions (PACs, LMEs, ...) are supported
  
  + *(Bug)* Occasional fail for matrix interpolation using new version of `R` package `interp` instead of `akima`.
     
  + *(Update)* Sidebar is on "Map" tab only.
  
  + *(New)* Ordered table with prior incompatibility.
  
+ 2021-06-05

  + Geranium is released on [wwfrussia](https://wwfrussia.shinyapps.io/geranium/) shinyapps account.

### Check scrolling

```{r, results='asis'}
cat(readLines("https://loripsum.net/api/plaintext/long/4"),sep="\n\n")
```
